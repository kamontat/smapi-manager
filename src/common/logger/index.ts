import Logger from "./model";
import Global from "./global";

export { Global, Logger };
export { isAcceptedLevel } from "./utils";
export { SILENT, ERROR, WARN, INFO, DEBUG } from "./level";

export type { Level } from "./level";
